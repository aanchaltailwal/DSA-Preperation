#include<stdio.h>
int main(){
    int a;
    scanf("%d", &a);

    for(a=0; a<=20; a++);

    if(a / a ==0 || a/2 == 0){
        printf("The prime no");

    } 
    else{
        printf("not a prime no");
    }
    return 0;
}